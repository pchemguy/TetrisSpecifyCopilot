{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main desktop window and local persistence commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}